# Domain Docs

Single-context repo. Before exploring, read `CONTEXT.md` and `docs/adr/` if they exist.

If they do not exist yet, proceed silently. Skills like `/domain-modeling` can create them when needed.
